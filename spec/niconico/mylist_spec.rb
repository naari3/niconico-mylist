# frozen_string_literal: true

RSpec.describe Niconico::Mylist do
  it 'has a version number' do
    expect(Niconico::Mylist::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
